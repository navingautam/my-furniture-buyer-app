# Copy this file to .env and fill in the real values.
# .env is already in .gitignore so it never gets committed.

API_KEY=

# Get this by POSTing your registered event email to /claim (see the Day 1
# Participant Guide) — the response includes your user_id.
PARTICIPANT_USER_ID=
